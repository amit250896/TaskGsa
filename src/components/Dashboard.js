import React from 'react';
import TodoList from './TodoList';

function Dashboard() {
  return (
    <div>
      <h2>Dashboard</h2>
      <TodoList />
      {/* Pagination component */}
      {/* Filter component */}
      {/* Add, edit, delete todo item forms */}
    </div>
  );
}

export default Dashboard;
